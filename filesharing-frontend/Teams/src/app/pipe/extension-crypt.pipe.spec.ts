import { ExtensionCryptPipe } from './extension-crypt.pipe';

describe('ExtensionCryptPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtensionCryptPipe();
    expect(pipe).toBeTruthy();
  });
});
